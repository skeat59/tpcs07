using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace myFarm
{
    class Farm : Drawable
    {
        int size_x;
        int size_y;
        int[,] grounds = new int[10, 10];
        static void array(int[,] tab)
        {
            for (int i = 0; i < tab.GetLength(0); i++)
            {
                for (int j = 0; j < tab.GetLength(1); j++)
                {
                    
                }
            }
        }
    }
}